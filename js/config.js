/* Charlie's Class — Supabase connection.
   1. Create a free project at https://supabase.com
   2. SQL Editor → paste supabase/schema.sql → Run
   3. Project Settings → API → copy the two values below.
   While both are empty the site runs in Local demo mode
   (data stays in this browser only — fine for trying it out). */
window.CHARLIE_CONFIG = {
  SUPABASE_URL: "",      // e.g. "https://abcdefgh.supabase.co"
  SUPABASE_ANON_KEY: ""  // the long "anon public" key
};
